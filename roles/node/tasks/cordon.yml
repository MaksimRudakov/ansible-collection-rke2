---
- name: Cordon node
  ansible.builtin.command:
    cmd: "{{ rke2_kubectl }} --kubeconfig {{ rke2_kubeconfig }} cordon {{ rke2_node_name }}"
  delegate_to: "{{ rke2_kubectl_delegate }}"
  changed_when: true
